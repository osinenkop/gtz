# Partial-result draft

This folder contains a standalone arXiv-style draft for the GTZ `(6,3)` partial
results.

Main file:

- `main.tex`

Build:

```bash
make
```

The paper deliberately does **not** claim a proof of GTZ `(6,3)`.  Exact,
computer-assisted statements are separated from numerical KKT-screen evidence.

